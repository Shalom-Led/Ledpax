<odoo>
    <data>     
        <record id="email_template_edi_porder_done" model="mail.template">
            <field name="name">Purchase Order: To Approve</field>
            <field name="model_id" ref="purchase.model_purchase_order"/>
            <field name="subject">${object.company_id.name} Order (Ref ${object.name or 'n/a' })</field>
            <field name="email_from">${object.sudo().create_uid.email_formatted | safe}</field>
            <field name="partner_to">${object.partner_id.id}</field>
            <field name="body_html" type="html">
            <div style="margin: 0px; padding: 0px;">
                <p style="margin: 0px; padding: 0px; font-size: 13px;">
                    Dear ${object.user_id.name}
                    % if object.user_id.parent_id:
                        (${object.user_id.parent_id.name})
                    % endif
                    <br/><br/>
                    Here is in attachment a purchase order <strong>${object.name}</strong>
                    % if object.partner_ref:
                        with reference: ${object.partner_ref}
                    % endif
                    amounting in <strong>${format_amount(object.amount_total, object.currency_id)}</strong>
                    from ${object.company_id.name}.
                    <br/><br/>
                    Please Approve following purchase order :  
                    <br></br> 
                    <center>
                    <a href="http://35.193.75.70:8069/web#id=${object.id}&amp;action=388&amp;model=purchase.order&amp;view_type=form&amp;menu_id=215" style="background-color: #1abc9c; padding: 20px; text-decoration: none; color: #fff; border-radius: 5px; font-size: 16px;" class="o_default_snippet_text">To Approve</a></center>
                    <br></br>
                    Best regards,
                </p>    
            </div>
            </field>
        </record> 
    </data>
</odoo> 

